/**
 Doblar números
numeros = [1, 2, 3, 4, 5];

Usá map para obtener un nuevo array con el doble de cada número.

 */

 let numeros = [1, 2, 3, 4, 5];

 let doble = numeros.map(n => n*2);

 console.log(doble);